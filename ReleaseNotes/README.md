# Release Notes (GitHub)

Vor jedem neuen Version-Tag `vX.Y.Z` eine Datei **`vX.Y.Z.md`** in diesem Ordner anlegen. Der Release-Workflow (`.github/workflows/release.yml`) bindet sie als Beschreibung des GitHub-Releases ein.
