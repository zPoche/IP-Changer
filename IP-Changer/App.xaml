<Application x:Class="ProfileIpSwitcher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Cobalt.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Lesbare Buttons: kein Auto-GROSSBUCHSTABEN, hoher Kontrast auf Dark-Theme -->
            <CharacterCasing x:Key="MahApps.CharacterCasing.Button">Normal</CharacterCasing>
            <CharacterCasing x:Key="MahApps.CharacterCasing.Button.Square">Normal</CharacterCasing>
            <CharacterCasing x:Key="MahApps.CharacterCasing.Button.Dialogs">Normal</CharacterCasing>

            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Foreground" Value="#FFF5F5F5" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray8}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray6}" />
            </Style>

            <Style x:Key="App.Button.Accent" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Foreground" Value="#FFFFFFFF" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Background" Value="#FF4EA8FF" />
                <Setter Property="BorderBrush" Value="#FF74BCFF" />
            </Style>

            <Style x:Key="App.Button.Subtle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Foreground" Value="#FFF3F3F3" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray8}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray6}" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="MinHeight" Value="32" />
            </Style>

            <!-- Gemeinsame UI-Bausteine (Karten, Überschriften) -->
            <Style x:Key="App.SectionCard" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray9}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray7}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="18,16" />
                <Setter Property="Margin" Value="0,0,0,16" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
            </Style>
            <Style x:Key="App.SectionTitleRow" TargetType="StackPanel">
                <Setter Property="Orientation" Value="Horizontal" />
                <Setter Property="Margin" Value="0,0,0,12" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
